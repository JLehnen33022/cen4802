public class Main
{
    /**
     * nthterm if 0 returns 0, loop iterates n-1 for each iteration.
     * @param n
     * @return
     */
    public static int nthterm(int n){
        if(n==0)return 0;
        if(n==1 || n==2) return 1;
        return nthterm(n-1)+nthterm(n-2);
    }
    /**
     * output of the class, dictating how the result is presented.
     * @param args
     */
    public static void main(String[] args) {
        int n=10;
        System.out.println("The "+n+"th term of Fibonacci sequence is "+nthterm(n));
    }
}
